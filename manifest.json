{
    "name": "Cert Provider",
    "version": "1.0",
    "description": "Get certs pre-loaded from pre-configured sources for sites",
    "manifest_version": 3,
    "background": {
        "scripts": ["background.js"]
    },
    "permissions": ["webRequest"]
  }